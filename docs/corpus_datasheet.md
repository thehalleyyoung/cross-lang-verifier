# Corpus datasheet

Generated by `ub_oracle.corpus_datasheet`; do not hand-edit the tables. Run `make corpus-datasheet-check` to assert this page and the JSON manifest are fresh.

Included records are persistent corpora, generated corpus manifests, and corpus-backed benchmark populations.  Pure execution studies that do not own a subject population are referenced as validation commands instead of being double-counted as separate datasets.

**Schema:** `corpus-datasheet/v1`
**Content hash:** `9b194f9768ef8c60b74df3310c80e1f63d75c3f9cfd4cdd0203655b0c287fb5f`
**Records:** 14
**Language pairs:** 5
**Divergence classes/families:** 29

| corpus | kind | population | pairs | balance | validation |
|---|---:|---:|---|---|---|
| `adversarial-near-misses` | adversarial corpus | 23 near-miss verifier case | `c->c`, `c->go`, `c->rust`, `rust->go` | `divergent_control`=10, `loud_abstention`=4, `safe_near_miss`=9 | `make adversarial-corpus-check` |
| `bug-regressions` | regression corpus | 1 frozen finding regression | `c->rust` | `regression`=1 | `make bug-regression-check` |
| `c2rust-output` | machine-translated corpus | 12 c2rust extraction unit | `c->rust` | `candidate`=8, `no_divergence_found`=4 | `make c2rust-corpus-check` |
| `divergence-zoo` | catalogue corpus | 36 zoo exhibit | `c->go`, `c->rust`, `c->swift` | `divergent`=22, `equivalent`=14 | `python3 -m src.ub_oracle.divergence_zoo` |
| `existing-tools-head-to-head` | benchmark over c2rust-output corpus | 12 c2rust subject reused for baseline comparison | `c->rust` | `expected_divergent`=8, `safe_control`=4 | `make existing-tools-check` |
| `github-mined-ports` | real-port extraction corpus | 3 mined port extraction sample | `c->rust` | `candidate`=2, `no_divergence_found`=1 | `make github-port-mining-check`<br>`make real-bug-check` |
| `historical-cve-replays` | weakness replay corpus | 60 CVE-tagged weakness replay | `c->go`, `c->rust` | `CWE-125`=8, `CWE-190`=8, `CWE-191`=8, `CWE-369`=8, `CWE-476`=8, `CWE-682`=6, `CWE-758`=6, `CWE-787`=8 | `make historical-cve-check` |
| `idiomatic-ports` | idiomatic port corpus | 21 source function x target language | `c->go`, `c->rust` | `divergent`=13, `equivalent`=8 | `make idiomatic-port-check` |
| `large-scale-generated` | large-scale stress corpus | 60000 generated translation pair | `c->go`, `c->rust` | `divergent`=29600, `equivalent`=30400 | `make large-scale-check` |
| `llm-translations` | LLM translation corpus | 216 frozen C-to-Rust translation | `c->rust` | `divergent`=120, `equivalent`=96 | `make llm-scale-check` |
| `multi-pair-translations` | multi-pair corpus | 15 source function x target language | `c->go`, `c->rust`, `c->swift` | `divergent`=9, `equivalent`=6 | `python3 -m src.ub_oracle.multipair_corpus` |
| `negative-true-equivalence` | negative corpus | 1000 true-equivalence port | `c->go`, `c->rust` | `go`=500, `rust`=500 | `make negative-corpus-check` |
| `public-leaderboard` | open benchmark leaderboard | 120 leaderboard case | `c->go`, `c->rust` | `divergent`=60, `equivalent`=60 | `make leaderboard-check` |
| `responsible-findings` | finding ledger | 1 responsible disclosure finding | `c->rust` | `confirmed_extraction_unit`=1 | `make real-bug-check` |

## Records

### `adversarial-near-misses` -- Hand-crafted adversarial near-miss corpus

**Provenance.** Hand-crafted boundary cases from internal red-team pressure points: divergent controls, safe one-edit twins, and loud abstentions.

**Construction and balance.** The committed manifest uses deterministic no-toolchain verdicts; live compiler replay separately ensures divergent controls do not degrade to safe or uncovered verdicts. Population: 23 near-miss verifier case; labels/balance: `divergent_control`=10, `loud_abstention`=4, `safe_near_miss`=9; classes/families: `array_oob`, `division_by_zero`, `intmin_div_neg1`, `probe_routing`, `shift_oob`, `signed_overflow`, `strict_aliasing`, `unsupported_pair`.

**Validation.** When clang/UBSan and a target compiler are available, divergent controls are replayed and must remain flagged. Commands: `make adversarial-corpus-check`.

**Artifacts.** `experiments/adversarial_corpus/adversarial_corpus.json`, `docs/adversarial_corpus.md`. Hash: `e79987d9d9c236e3f3bef1f7f1c423c612bc350d09b8b33d4e5a2c57c8029f72`.

**Limitations.** Small by design; it stress-tests boundaries rather than estimating population rates. The byte-stable manifest intentionally excludes host-dependent live replay output.

### `bug-regressions` -- Frozen regression corpus from caught divergences

**Provenance.** Frozen regressions are confirmed extraction-unit divergences derived from the responsible findings lane; they are not upstream CVEs or repository-defect claims without a separate upstream source audit.

**Construction and balance.** Every responsible finding is mirrored into a regression entry with source hashes, witness input, safe input, and a reproduction bundle. Population: 1 frozen finding regression; labels/balance: `regression`=1; classes/families: `signed_overflow`.

**Validation.** The gate lints bundles, checks frozen source hashes, and replays the live witness when clang/UBSan plus the target compiler are available. Commands: `make bug-regression-check`.

**Artifacts.** `experiments/bug_regressions/bug_regression_corpus.json`, `docs/bug_regression_corpus.md`. Hash: `99ea4ff4cd6bcfc9d1527f49519935c19be255f6282cc4016ac9618ffa17ca7b`.

**Limitations.** The corpus size follows the number of responsibly recorded findings. It inherits the upstream-claim restrictions from the findings ledger.

### `c2rust-output` -- Tier-1 c2rust-output extraction corpus

**Provenance.** Twelve real-library extraction families are transpiled by the actual c2rust C2Rust 0.22.1 binary.

**Construction and balance.** Checked-in C extraction units are regenerated into Rust with c2rust; the verifier records symbolic verdicts and source/target hashes. Population: 12 c2rust extraction unit; labels/balance: `candidate`=8, `no_divergence_found`=4; classes/families: `div_by_zero`, `intmin_div_neg1`, `shift_oob`, `signed_overflow`.

**Validation.** Focused tests compile every C source and generated Rust artifact; when c2rust is installed, checked-in Rust is byte-compared to fresh output. Commands: `make c2rust-corpus-check`.

**Artifacts.** `experiments/c2rust_corpus/results.json`, `experiments/c2rust_corpus/sources`, `experiments/c2rust_corpus/generated`. Hash: `982489dd8ce14b05b115533fa14c97f0cbe35b769856c463e3833314903f044e`.

**Limitations.** Extraction units are minimized families, not vendored full upstream trees. Compiler-backed regeneration requires a local c2rust binary.

### `divergence-zoo` -- Machine-readable divergence zoo

**Provenance.** Generated from the live idiomatic and multi-pair corpora, preserving each exhibit's source, translation, witness, safe input, and provenance.

**Construction and balance.** The zoo indexes divergent exhibits by class and pair and regenerates docs/zoo.md from corpus data. Population: 36 zoo exhibit; labels/balance: `divergent`=22, `equivalent`=14; classes/families: `div_by_zero`, `memcpy_overlap`, `none`, `oversized_shift`, `signed_overflow`, `uninit_padding`.

**Validation.** confirm_zoo re-runs every available divergent witness and safe input through the real compiler-backed harness. Commands: `python3 -m src.ub_oracle.divergence_zoo`.

**Artifacts.** `docs/zoo.md`. Hash: `2eaea983dfa45403`.

**Limitations.** The zoo is a curated reference index, not a statistically sampled benchmark. Live reconfirmation is consistency-only when no target toolchain is present.

### `existing-tools-head-to-head` -- Same-corpus existing-tools benchmark

**Provenance.** Benchmark subjects are the checked Tier-1 c2rust-output corpus; this record is labeled as a benchmark to avoid double-counting the c2rust population.

**Construction and balance.** Each subject is wrapped into runnable C/Rust programs and evaluated against SemRec, c2rust-style tests, optional Miri, and equal-budget fuzzing. Population: 12 c2rust subject reused for baseline comparison; labels/balance: `expected_divergent`=8, `safe_control`=4; classes/families: `div_by_zero`, `intmin_div_neg1`, `shift_oob`, `signed_overflow`.

**Validation.** The focused gate confirms SemRec on the real c2rust subjects and runs the baseline checks with the same corpus inputs when toolchains exist. Commands: `make existing-tools-check`.

**Artifacts.** `src/ub_oracle/existing_tools_study.py`. Hash: `dd3950d3b7dfcde1a4b01729`.

**Limitations.** Miri is Rust-only and is recorded as structural applicability, not as a cross-language oracle. This benchmark reuses c2rust subjects and should not be counted as an independent corpus.

### `github-mined-ports` -- GitHub-mined active C-to-Rust port samples

**Provenance.** Seeded and optionally live-ranked public C-to-Rust port families including coreutils, sudo-rs, and zlib-rs.

**Construction and balance.** Repository candidates are scored deterministically; checked-in extraction samples are verified through the same symbolic verdict layer. Population: 3 mined port extraction sample; labels/balance: `candidate`=2, `no_divergence_found`=1; classes/families: `div_by_zero`, `signed_overflow`.

**Validation.** The checked samples compile as real C/Rust code when toolchains are available; the divergent sudo/coreutils-style samples are compiler-confirmed through the verifier lane. Commands: `make github-port-mining-check`, `make real-bug-check`.

**Artifacts.** `experiments/github_ports/results.json`, `experiments/github_ports/samples`. Hash: `6393600c4d6400d2d8ba95ee45c75ce869ababc45afd5d3b3ce37b50c4645f4a`.

**Limitations.** Samples are extraction units derived from port families, not whole-repo audits. The live GitHub search path is optional and not part of byte-stable CI.

### `historical-cve-replays` -- Historical CVE weakness-class replay corpus

**Provenance.** CVE IDs and NVD CWE families are used as weakness-class provenance; replays are minimized examples, not original vulnerable vendor source.

**Construction and balance.** Each historical CVE entry points to a replay template, NVD CWE query, per-language target source, witness inputs, and safe controls. Population: 60 CVE-tagged weakness replay; labels/balance: `CWE-125`=8, `CWE-190`=8, `CWE-191`=8, `CWE-369`=8, `CWE-476`=8, `CWE-682`=6, `CWE-758`=6, `CWE-787`=8; classes/families: `division_by_zero`, `int_min_div_neg1`, `null_deref`, `out_of_bounds_read`, `out_of_bounds_write`, `oversized_shift`, `signed_overflow`, `signed_underflow`.

**Validation.** Representative templates and bundles are compiled and replayed against real clang/UBSan plus rustc/go when those toolchains are available. Commands: `make historical-cve-check`.

**Artifacts.** `src/ub_oracle/cve_corpus.py`. Hash: `631d6e35b8e06a832b5270bc`.

**Limitations.** The corpus reproduces weakness classes associated with CVEs; it is not original vulnerable vendor source or vendored third-party source.

### `idiomatic-ports` -- Human-idiomatic port corpus

**Provenance.** Real-world-shaped human ports, including coreutils/sudo-rs/zlib-rs classes, with non-literal Rust and Go translations.

**Construction and balance.** Each item carries C source, idiomatic targets, a UB-triggering input, and a safe input; Step 161 expands the real-port family coverage. Population: 21 source function x target language; labels/balance: `divergent`=13, `equivalent`=8; classes/families: `div_by_zero`, `memcpy_overlap`, `none`, `oversized_shift`, `signed_overflow`, `uninit_padding`.

**Validation.** Focused tests compile and replay Step 161 items through real clang/UBSan, rustc, and go when available. Commands: `make idiomatic-port-check`.

**Artifacts.** `src/ub_oracle/idiomatic_corpus.py`. Hash: `7b633e0eb673b1ed95f8befc`.

**Limitations.** Functions are extraction units, not whole applications. The corpus emphasizes integer/runtime-contract divergences and true controls.

**Notes.** Step 161 families: coreutils-block-rounding, sudo-rs-timeout-slice, zlib-rs-adler-window

### `large-scale-generated` -- 1M-LOC generated migration stress corpus

**Provenance.** Mechanically generated, distinct, compilable C-to-Rust/C-to-Go programs over catalogued divergence and safe-control families.

**Construction and balance.** The full corpus census counts exact LOC, distinct-program hashes, label balance, pair balance, and class balance; live runs execute a deterministic seeded sample. Population: 60000 generated translation pair; labels/balance: `divergent`=29600, `equivalent`=30400; classes/families: `div_by_zero`, `oob_read`, `oversized_shift`, `safe_add`, `safe_mod`, `safe_mul`, `safe_shift`, `signed_overflow`.

**Validation.** The live sample is labeled by real clang/UBSan plus rustc/go when available, with a reproducible verdict-layer hash for a fixed seed. Commands: `make large-scale-check`.

**Artifacts.** `src/ub_oracle/large_scale_study.py`. Hash: `6b45fc7a1743c73ec3604ae8`.

**Limitations.** The scale population is generated rather than mined from third-party repositories. CI normally samples the population instead of compiling every item.

**Notes.** total_loc=1044000

### `llm-translations` -- Frozen LLM-transpiler scale corpus

**Provenance.** Frozen GitHub Copilot CLI/GPT-5.5 translations derived from the Tier-1 c2rust real-library extraction families.

**Construction and balance.** Deterministic fixtures retain prompt/model provenance and are labeled only through real clang/UBSan plus rustc execution in the study gate. Population: 216 frozen C-to-Rust translation; labels/balance: `divergent`=120, `equivalent`=96; classes/families: `array_oob`, `div_by_zero`, `intmin_div_neg1`, `safe_control`, `shift_oob`, `signed_overflow`, `signed_underflow`.

**Validation.** The focused gate checks corpus size, hash stability, Wilson intervals, and a seeded live real-compiler sample. Commands: `make llm-scale-check`.

**Artifacts.** `src/ub_oracle/llm_scale_study.py`. Hash: `9a37617cfea73cc059bb5277`.

**Limitations.** The translations are frozen fixtures, not an online model benchmark. Reported live metrics depend on local compiler availability.

### `multi-pair-translations` -- Tier-3 multi-pair translation corpus

**Provenance.** C functions translated into Rust, Go, and Swift in deliberately varied transpiler/LLM-style forms to test cross-pair invariance.

**Construction and balance.** Divergent functions must flag on every available target; equivalent functions must stay silent across all target pairs. Population: 15 source function x target language; labels/balance: `divergent`=9, `equivalent`=6; classes/families: `div_by_zero`, `none`, `oversized_shift`, `signed_overflow`.

**Validation.** Live confirmation compiles and runs clang/UBSan plus every available target compiler and requires a stable verdict hash across runs. Commands: `python3 -m src.ub_oracle.multipair_corpus`.

**Artifacts.** `src/ub_oracle/multipair_corpus.py`. Hash: `3b812cebe972e617e2c11e38`.

**Limitations.** Swift coverage is opportunistic and depends on local toolchain availability. The corpus is a focused generality check rather than a large-scale scrape.

### `negative-true-equivalence` -- Verified-equivalent negative corpus

**Provenance.** Deterministically generated C-to-Rust/C-to-Go safe integer ports with declared operating ranges.

**Construction and balance.** All items are covered by registered oracles, range-pruned, and checked against bounded proof inputs to bound false-positive behavior. Population: 1000 true-equivalence port; labels/balance: `go`=500, `rust`=500; classes/families: `safe_add_const`, `safe_div`, `safe_rem`, `safe_shift`, `safe_sub_const`.

**Validation.** A seeded sample is labeled equivalent by real clang/UBSan plus rustc/go when the toolchains are available. Commands: `make negative-corpus-check`.

**Artifacts.** `experiments/negative_corpus/negative_corpus.json`, `docs/negative_corpus.md`. Hash: `218b7f573e1634b7dc43cab63a54e41ba94b904f4f6acd333b51fc6ce1b95f97`.

**Limitations.** The corpus bounds false positives over its generated safe families; it is not a proof of arbitrary-program equivalence.

### `public-leaderboard` -- Public submission leaderboard split

**Provenance.** Deterministic held-back split from the sanitizer-backed Step-45 ground-truth corpus across C-to-Rust and C-to-Go programs.

**Construction and balance.** Public cases omit labels and include runnable source plus argv inputs; the frozen answer key enables reproducible auto-scoring and can be kept server-side by a hosted leaderboard. Population: 120 leaderboard case; labels/balance: `divergent`=60, `equivalent`=60; classes/families: `div_by_zero`, `oob_read`, `oversized_shift`, `safe_add`, `safe_index`, `safe_mod`, `safe_mul`, `safe_shift`, `signed_overflow`.

**Validation.** The focused gate proves artifact freshness and scorer edge cases; when clang/UBSan plus a target compiler are available, a seeded sample is re-labeled by real compiler execution. Commands: `make leaderboard-check`.

**Artifacts.** `experiments/leaderboard/public_cases.json`, `experiments/leaderboard/answer_key.json`, `experiments/leaderboard/sample_submission.json`, `docs/leaderboard.md`. Hash: `8763676749217e9bdb2fe9409f206b34788402680608181335e3e0dbca65436a`.

**Limitations.** The committed answer key is intentionally open for artifact reproducibility and is not an anti-cheat secret in a source checkout. The benchmark uses executable extraction programs rather than full upstream repositories.

### `responsible-findings` -- Evidence-tiered responsible divergence findings

**Provenance.** Actively maintained public port families with maintainer-ready extraction-unit findings and explicit upstream-claim boundaries.

**Construction and balance.** Symbolic witnesses are attached to frozen source hashes, safe controls, maintenance snapshots, and reproduction bundles. Population: 1 responsible disclosure finding; labels/balance: `confirmed_extraction_unit`=1; classes/families: `signed_overflow`.

**Validation.** When clang/UBSan and rustc are available, every finding witness traps on C and is defined on the Rust target while the safe control stays silent. Commands: `make real-bug-check`.

**Artifacts.** `experiments/github_ports/divergence_findings.json`, `docs/divergence_findings.md`, `docs/repro/`. Hash: `30227538c516d5a9a769b826`.

**Limitations.** Findings are not cited as upstream CVEs or confirmed repository defects until a separate source audit confirms the same pattern upstream.
