# Divergence findings — responsible evidence tier

This page is generated by `ub_oracle.divergence_findings`. It records newly confirmed divergence findings from actively maintained C->Rust port families while separating three evidence tiers:

1. **Confirmed extraction-unit finding:** the checked-in minimized C and Rust functions compile and reproduce the divergence against real `clang`/UBSan and `rustc`.
2. **Maintainer-ready disclosure draft:** the bundle is suitable for a coordinated report once the same pattern is audited in upstream source.
3. **Upstream defect claim:** intentionally **not** made here until that source audit is complete.

## CLV-DIV-0001 — `coreutils-size-accumulate` (signed_overflow)

- **Repository family:** [uutils/coreutils](https://github.com/uutils/coreutils)
- **Active-maintenance evidence:** GitHub reported a same-day push and update when this finding was prepared; the repository was not archived. (snapshot source: `gh repo view uutils/coreutils --json nameWithOwner,pushedAt,updatedAt,isArchived,stargazerCount,url,description`; pushed at `2026-06-03T08:05:36Z`; archived = `False`)
- **Evidence tier:** confirmed extraction-unit finding
- **Upstream status:** upstream-instance source audit pending; this record must not be cited as an upstream uutils/coreutils CVE or confirmed repository defect
- **Disclosure status:** maintainer-ready coordinated-disclosure draft generated; send only after auditing the upstream source for the same pattern
- **Live reproduction status:** reproduced live
- **Witness / safe input:** `2147479552` / `0`
- **Reproduction bundle:** `docs/repro/CLV-DIV-0001.sh`
- **Checked sources:** `experiments/github_ports/samples/coreutils_size_accumulate.c` (`300fb32daa3d`), `experiments/github_ports/samples/coreutils_size_accumulate.rs` (`c6ceb0fa4fa7`)
- **Remediation:** Accumulate byte counts in an unsigned or widened type, or guard the maximum representable value before translating to wrapping target arithmetic.

**Why it matters.** The C extraction unit executes undefined behavior when the byte counter crosses the signed 32-bit limit, while the Rust extraction unit uses explicit wrapping arithmetic and returns a deterministic value on the same input. A port that preserves this shape has made a latent C signed-overflow precondition into defined target behavior; the fix is to make the counter range and overflow policy explicit before translation.

Content hash: `05970d6267360a04`.
