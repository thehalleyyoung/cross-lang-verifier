# Frozen bug-regression corpus

This page is generated by `ub_oracle.bug_regression_corpus`. It freezes each responsibly recorded divergence finding into a regression case with pinned source hashes, witness inputs, a safe control, and a checked reproduction bundle.

The corpus inherits the evidence policy from `docs/divergence_findings.md`: these are confirmed extraction-unit divergences, not upstream CVEs or repository-defect claims without a separate upstream source audit.

- **Schema:** `bug-regression-corpus/v1`
- **Regressions:** 1
- **Content hash:** `99ea4ff4cd6bcfc9d1527f49519935c19be255f6282cc4016ac9618ffa17ca7b`

## CLV-DIV-0001 — `coreutils-size-accumulate`

- **Pair / class:** C -> rust / `signed_overflow`
- **Repository family:** [uutils/coreutils](https://github.com/uutils/coreutils)
- **Witness / safe input:** `2147479552` / `0`
- **Frozen sources:** `experiments/github_ports/samples/coreutils_size_accumulate.c` (`300fb32daa3d`), `experiments/github_ports/samples/coreutils_size_accumulate.rs` (`c6ceb0fa4fa7`)
- **Reproduction bundle:** `docs/repro/CLV-DIV-0001.sh`
- **Upstream status:** upstream-instance source audit pending; this record must not be cited as an upstream uutils/coreutils CVE or confirmed repository defect

**Regression contract.** The focused gate recomputes source hashes, checks that this finding still appears in `finding_records()`, lints the existing bundle, and, when `clang`/UBSan plus the target compiler are available, replays the witness and safe control on real code.

Run `make bug-regression-check` to validate the frozen corpus.
